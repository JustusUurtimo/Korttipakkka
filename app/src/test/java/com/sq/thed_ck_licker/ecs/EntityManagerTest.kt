package com.sq.thed_ck_licker.ecs

import org.junit.Test

class EntityManagerTest {

//    @Test
//    fun `getPlayerID   returns correct ID`() {
//        // Check if getPlayerID() always returns the predefined PLAYER_ID constant which is 1.
//        assert(EntityManager.getPlayerID() == 1) { "getPlayerID should return 1" }
//    }


//    @Test
//    fun `createNewEntity   returns correct first ID`() {
//        // Verify that the first call to createNewEntity() returns 2.
//        val entityId = EntityManager.createNewEntity()
//        assert(entityId == 2) { "First entity ID should be 2, was $entityId" }
//    }

//    @Test
//    fun `createNewEntity   ID increment`() {
//        // Verify that the returned ID from createNewEntity is one more
//        //than the previous one
//        for (i in 3..10) {
//            val entityId = EntityManager.createNewEntity()
//            assert(entityId == i) { "First entity ID should be ${i}, was $entityId" }
//        }
//    }


//    @Test

//    fun `createNewEntity   multiple calls`() {
//        // Create multiple new entities (e.g., 100) and verify
//        // that each entity's ID is unique and properly incremented.
//        for (i in 11..100) {
//            val entityId = EntityManager.createNewEntity()
//            assert(entityId == i) { "First entity ID should be ${i}, was $entityId" }
//        }
//    }


}